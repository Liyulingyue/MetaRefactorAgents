# LLM Settings
OPENAI_API_KEY=EMPTY
OPENAI_API_URL=https://api.openai.com/v1
OPENAI_MODEL_NAME=gpt-4o-mini

# Server Settings
HOST=0.0.0.0
PORT=8000

# Workspace Settings
WORKSPACE_ROOT=./workspace
ACTIVE_TEMPLATE=mcp_tools_20260613

# Feishu Settings (optional)
FEISHU_APP_ID=
FEISHU_APP_SECRET=
FEISHU_BOT_NAME=MRA Bot
FEISHU_WEBHOOK_PATH=/feishu/webhook

# Skills & Plan Injection Mode
# static: load once at startup (low cost)
# dynamic: re-evaluate every LLM call (reflects latest state)
SKILLS_INJECTION_MODE=static
PLAN_INJECTION_MODE=dynamic

# MCP Settings
# static: load MCP tools once at startup, use reload_mcp_tools tool to reload
# dynamic: re-load MCP tools every LLM call (auto-refresh)
MCP_INJECTION_MODE=static
