# Agent 模板更新建议 (default_20260406)

针对当前 `Agent-01` 和 `Agent-02` 的运行日志分析，现对 `template/default_20260406` 提出以下系统级升级建议，以解决在大文件操作、资源损耗、日志管理以及协作效率方面的本质问题。

## 1. 核心工具升级 (app/core/tools.py)

### 1.1 引入原子化编辑工具 (Strategic Edits)
**现状不足**：目前 Agent 仅有 `write_file`（全量覆盖），导致修改 30KB 文件时产生巨大 Token 浪费且易断连。
**建议新增**：
- `replace_string_in_file(file_path, old_string, new_string)`：支持基于字符串匹配的精确替换，减少输出量。
- `append_to_file(file_path, content)`：支持在文件末尾追加，适用于日志或长文档续写。

### 1.2 引入智能文件读取 (Smart Context)
**现状不足**：Agent 直接 `read_file` 读取 600KB+ 的日志，瞬间填满上下文导致失忆。
**建议新增**：
- `read_file_range(file_path, start_line, end_line)`：支持按行号读取。
- `tail_file(file_path, num_lines)`：快速获取日志末尾状态，避免读取历史冗余。
- `grep_file(file_path, pattern)`：在文件中搜索特定关键词（如 "Error" 或 "USER"）。

### 1.3 升级协作协议 (Broadcast System)
**现状不足**：Agent 之间通过硬编码路径（`../Agent-01/`）偷窥日志，耦合度高且不安全。
**建议新增**：
- `broadcast_event(event_type, message)`：将重要事件（如“我完成了专利初稿”）写入 `world_log.md`，实现解耦式协作（遵循用户 Memory 中的大事记设计）。
- `pray(wish)`：Agent 向上帝（用户）发送愿望，收集到 `shrine/prayers.md`。

---

## 2. 日志与生命周期管理 (app/core/manager.py & run.py)

### 2.1 日志自动滚动与归档
**现状不足**：`thoughts.md` 无限制膨胀，成为系统的“上下文地雷”。
**建议改进**：
- 实现日志轮转机制：当 `thoughts.md` 超过 100KB 时，自动重命名并归档至 `logs/archived/`。
- 在 `run.py` 中引入异常拦截：记录 Python 未捕获的 Exception 到特定的 `error.log`，不再在 `thoughts.md` 中草草带过。

### 2.2 状态缓存 (Session Cache)
**现状不足**：Agent 重复执行 `find` 和 `ls` 扫描环境。
**建议改进**：
- 在 `AgentTools` 中引入简单的结果缓存机制，在同一对话轮次内，静态环境扫描结果（如 `list_peers`）应在 30 秒内复用缓存。

---

## 3. 环境配置与安全 (app/core/config.py)

### 3.1 路径隔离增强
**现状不足**：`write_file` 的路径检查过于宽松。
**建议改进**：
- 严格限制 `write_file` 仅能操作：
    1. 当前 Agent 目录。
    2. `workspace/.shared_files/` 目录。
    3. 显式授予权限的其他 Agent `workspace/` 目录。

---

## 4. 给 opencode 的升级指令建议

请 opencode 执行以下具体操作：
1.  **修改 `app/core/tools.py`**：添加上述提到的 `replace_string_in_file`, `read_file_range`, `broadcast_event` 工具及其对应的 Schema。
2.  **修改 `run.py`**：增加对 `error.log` 的单独处理和更详细的日志格式定义。
3.  **在 `app/core/` 下新建 `intelligence.py`**：负责异步的日志归档逻辑。
4.  **更新 `requirements.txt`**：引入 `python-dotenv` 和 `rich`（用于日志美化，辅助 Agent 区分关键信息）。

---
*版本：v1.1.0-beta*
*维护人：GitHub Copilot*
